import { StyleSheet } from 'react-native';
import { primaryColor, textSize } from '../../../stylingVariables';

const styles = StyleSheet.create({
  forgotPassword: {
    color: `${primaryColor}`,
    fontSize: textSize,
  },
});

export default styles;
